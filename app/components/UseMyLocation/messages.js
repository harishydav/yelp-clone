/*
 * UseMyLocation Messages
 *
 * This contains all the text for the UseMyLocation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UseMyLocation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UseMyLocation component!',
  },
});
