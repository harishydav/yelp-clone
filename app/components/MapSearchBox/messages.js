/*
 * MapSearchBox Messages
 *
 * This contains all the text for the MapSearchBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MapSearchBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapSearchBox component!',
  },
});
